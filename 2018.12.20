1.完成快速定位日志服务器功能，记录一下遇到的问题。
  过程中遇到的问题：
  a.myCommand 进行了修改，用之前的会出现超时情况。
    以上程序运行时间很快，因此并没搞清楚有超时判断后为何无法正常运行
    猜测因为命令输出内容太多，需回头再研究原myCommand代码
   
  b.本想把获取的数据转换成字典，然后写入文件中。
    字典无法写入文件，因此需转换成json，写入文件正常。但明细有耗时，且文件大。
    因此在读文件时，效率很低。
    下去需要关注一下json数据处理方面。
    
 2.leetcode 编程题
 给定两个大小为 m 和 n 的有序数组 nums1 和 nums2。
请你找出这两个有序数组的中位数，并且要求算法的时间复杂度为 O(log(m + n))。
你可以假设 nums1 和 nums2 不会同时为空。
示例 1:
nums1 = [1, 3]
nums2 = [2]
则中位数是 2.0
示例 2:
nums1 = [1, 2]
nums2 = [3, 4]
则中位数是 (2 + 3)/2 = 2.5

自己解题思路：
不考虑时间下
两个数组相加
numpy.median(arr)

先判断，两个数组的头尾大小
